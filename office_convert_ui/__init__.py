"""Office Convert UI package — Streamlit dashboard.

Entry point: `streamlit run office_convert_ui/app.py`. The dashboard
talks to the API via HTTP (API_URL env var), not Python imports, so
this package has no compile-time dependency on `office_convert`.
"""

